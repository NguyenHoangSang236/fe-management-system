const HorizontalAlignment = Object.freeze({
    LEFT: 'left',
    CENTER: 'center',
    RIGHT: 'right',
});

const VerticalAlignment = Object.freeze({
    TOP: 'top',
    CENTER: 'center',
    BOTTOM: 'bottom',
});

const FlexDirection = Object.freeze({
    ROW: 'row',
    COLUMN: 'column',
});

export { HorizontalAlignment, VerticalAlignment, FlexDirection };